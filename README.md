# Psychedelic Society of Kansas City

Astro static site for the PSKC staging URL:
`https://heartlandtranspersonalalliance.github.io/pskc-staging/`.

## Adding Markdown Posts

All published posts currently use the `news` content collection.

| Type | Directory | URL |
| --- | --- | --- |
| News, press releases, advocacy updates, event recaps | `src/content/news/` | `/pskc-staging/<filename>/` |

Create a new `.md` or `.mdx` file in `src/content/news/`. The filename becomes
the page slug. For example:

```text
src/content/news/city-council-statement.md
```

publishes at:

```text
https://heartlandtranspersonalalliance.github.io/pskc-staging/city-council-statement/
```

Required frontmatter:

```md
---
title: "Post title"
date: 2026-05-01
description: "Short summary shown on the News page and used for metadata."
categories:
  - News
tags:
  - Kansas City
---
```

`title`, `date`, and `description` are required. `categories` and `tags` are
optional arrays, but using them keeps the News page easier to scan.

Images should be placed in `public/assets/images/news/`. Since this staging site
is deployed under `/pskc-staging/`, Markdown image paths should include that base:

```md
![Alt text](/pskc-staging/assets/images/news/example-photo.jpg)
```

Do not edit `dist/` directly. It is generated by the build.

## Publishing Workflow

1. Add or edit Markdown files in `src/content/news/`.
2. Run a local build:

```sh
nix develop -c npm run build
```

3. Commit the changes to `main`.
4. Push to GitHub.

Pushing to `main` runs `.github/workflows/astro.yml`, builds the Astro site, and
deploys it through GitHub Pages.

## Example: News Post

Use `categories: ["News"]` for general announcements or organizational updates.
This example shows headings, links, lists, a table, and image embedding.

```md
---
title: "PSKC Announces Spring Education Series"
date: 2026-05-01
description: "PSKC announces a fictional spring education series focused on harm reduction, integration, and community dialogue."
categories:
  - News
tags:
  - Education
  - Community
  - Harm Reduction
---

PSKC is launching a **fictional spring education series** for community members,
students, clinicians, artists, and organizers.

![Fictional audience at a PSKC education night](/pskc-staging/assets/images/news/spring-education-audience.jpg)

## Program Goals

- Share evidence-aware psychedelic education
- Build local peer connection
- Highlight practical harm reduction tools
- Invite questions from new and returning community members

Learn more through the [PSKC Lu.ma calendar](https://lu.ma/pskc).

| Session | Topic | Format |
| --- | --- | --- |
| April | Foundations of harm reduction | Talk and Q&A |
| May | Integration and peer support | Panel |
| June | Policy and public health | Community forum |

> "Public education works best when it is clear, compassionate, and grounded."
>
> <cite>Fictional PSKC Program Lead</cite>
```

## Example: Press Release

Use `categories: ["Press Release"]` for formal announcements. This example shows
contact blocks, bold lead text, raw HTML callouts, blockquotes, and numbered
sections. Raw HTML is supported in Markdown posts when a custom layout block is
helpful.

```md
---
title: "PSKC Issues Statement on Fictional Public Health Proposal"
date: 2026-05-01
description: "PSKC issues a fictional statement urging evidence-based public health policy and community input."
categories:
  - Press Release
  - Advocacy
tags:
  - Public Health
  - Drug Policy
  - Kansas City Council
---

**FOR IMMEDIATE RELEASE**<br>
May 1, 2026

**Contact:**<br>
Email: [info@psychedelickc.org](mailto:info@psychedelickc.org)<br>
Website: [www.psychedelickc.org](https://www.psychedelickc.org)

**KANSAS CITY, MO** - The Psychedelic Society of Kansas City is responding to a
fictional public health proposal with a call for evidence, compassion, and
community participation.

![Fictional press conference outside city hall](/pskc-staging/assets/images/news/city-hall-press-conference.jpg)

<div class="fact-box">
<p><strong>Key Facts at a Glance</strong></p>
<ul>
<li><strong>Status:</strong> Fictional example for README documentation</li>
<li><strong>Focus:</strong> Public health, safety, and community input</li>
<li><strong>Recommendation:</strong> Use evidence-based harm reduction</li>
</ul>
</div>

### PSKC's Position

1. Policies should be clear and enforceable.
2. Harm reduction tools should remain accessible.
3. Community stakeholders should be heard before final votes.

> "Kansas City deserves thoughtful public health policy, not rushed decisions."
>
> <cite>Fictional PSKC Board Member</cite>

### About PSKC

PSKC is a community organization focused on education, dialogue, harm reduction,
and responsible public conversation about psychedelics.
```

## Example: Advocacy Update or Event Recap

Use categories such as `Advocacy`, `Event Recap`, or `Community Update` when a
post is less formal than a press release. This example shows checklists, nested
lists, footnotes, inline code, and a linked image.

```md
---
title: "Fictional Recap: Community Harm Reduction Forum"
date: 2026-05-01
description: "A fictional recap of a PSKC community forum with discussion notes, next steps, and resource links."
categories:
  - Event Recap
  - Harm Reduction
tags:
  - Community Forum
  - Resources
---

PSKC hosted a fictional community forum on safer education practices and local
resource sharing.[^note]

[![Fictional resource table at a PSKC forum](/pskc-staging/assets/images/news/forum-resource-table.jpg)](https://lu.ma/pskc)

## Discussion Themes

- Practical education
  - Clear language
  - Reliable sources
  - Room for questions
- Community care
  - Peer support
  - Referral pathways
  - Inclusive spaces

## Follow-up Checklist

- [x] Publish a recap
- [x] Share resources with attendees
- [ ] Add next forum date to `src/content/news/`

Helpful resource links:

- [Fireside Project](https://firesideproject.org/)
- [DanceSafe](https://dancesafe.org/)
- [MAPS](https://maps.org/)

[^note]: This is a fictional example footnote for formatting reference.
```

## Development

```sh
npm install
npm run dev
```

With Nix:

```sh
nix develop
npm run dev
```

If you use `direnv`, run `direnv allow` once. After that, entering this repo will
load the Nix shell automatically through `.envrc`.

When `dist/` already exists, the direnv shell also starts a background Nginx
preview on `http://127.0.0.1:8080/` and prints any detected LAN URLs. Disable
that with `PSKC_AUTO_PREVIEW=0`.

## Build

```sh
npm run build
```

The staging build is configured for GitHub Pages project hosting with:

- Astro `site` set to `https://heartlandtranspersonalalliance.github.io`
- Astro `base` set to `/pskc-staging`
- trailing slashes enabled for generated routes

## Deployment

The repository uses GitHub Pages with **GitHub Actions** as the Pages source. The
workflow installs dependencies with `npm ci`, builds the site with `npm run
build`, uploads `dist/` as a Pages artifact, and deploys it.

This staging repo does not use a custom domain, so there is no `public/CNAME`
file.

## LAN Preview With Nginx

The flake provides `pskc-lan-preview`, a user-space Nginx wrapper for previewing
the generated Astro site on your local network.

```sh
nix develop
pskc-lan-preview
```

By default it:

- installs npm dependencies if `node_modules/` is missing
- runs `npm run build`
- serves `dist/` with Nginx on `0.0.0.0:8080`
- prints local and LAN URLs such as `http://127.0.0.1:8080/`

The foreground command blocks until you press Ctrl-C. For a background server:

```sh
pskc-lan-preview --daemon
pskc-lan-preview --status
pskc-lan-preview --stop
```

Useful overrides:

```sh
PSKC_PORT=8081 pskc-lan-preview
PSKC_BUILD=0 pskc-lan-preview
PSKC_HOST=127.0.0.1 pskc-lan-preview
PSKC_AUTO_PREVIEW=0 direnv reload
```
